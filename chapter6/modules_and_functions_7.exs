# Find the library functions to do the following and then use each in IEx
# - Convert a float to a string with two decimal digits (Erlang)
# - Get the value of an operating system environment variable (Elixir)
# - Return the extension component of a file name (so return .exs if given "dave/test.exs") (Elicir)
# - Return the process's current working directory (Elixir)
# - Convert a string containing JSON into Elixir data strucutres (Just find; don't install)
# - Execute a comman in your operating system's shell